<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>{{BLOG_TITLE}}</title>
  </head>
  <body>
    <h1>{{constant('App\\Sample\\Common::BLOG_TITLE') }}}</h1>
    <hr>
    <h2>Article List</h2>
    <ul>
    {% for user in airticle %}
        <li>
          {{ user.date|e}}
          <a href="show.php?id={{user.id}}">
            {{ user.title|e }}
          </a>
        </li>
    {% endfor %}
    </ul>

  </body>
</html>
